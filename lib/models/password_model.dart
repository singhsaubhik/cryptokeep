class Password {
  String id;
  String username;
  String password;
  String name;

  String createdAt, updatedAt;

  Password(
      {this.id,
      this.name,
      this.username,
      this.password,
      this.createdAt,
      this.updatedAt});
}
